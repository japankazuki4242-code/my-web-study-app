# my-web-study-app 開発ルール

このプロジェクトは `my-web-study-app` です。

## Version 1 の基本方針

Version 1では、HTML・CSS・JavaScript・localStorageを使用します。

Version 1では、Reactなどのフレームワークを使用しません。

初心者が読みやすく、理解しやすいコードを優先します。

不要に高度な書き方や、過度な抽象化を避けます。

現在はVersion 1の完成を優先します。
関連する変更は適切な単位でまとめて実装します。

Version 1の完成に直接必要のない新機能・大規模なリファクタリング・高度な最適化は行いません。

改善案があっても、Version 1の完成に必要ないものは勝手に実装せず、提案だけにします。


## 既存コード・既存機能の扱い

既存のデザイン・HTML構造・CSS・JavaScriptの動作を、できるだけ維持します。

1つの機能を追加・修正するために、他のページや既存機能を不用意に変更しません。

既存ファイルの大きな変更や削除は、実行前にユーザーへ確認します。

既存機能に大きな影響が出る可能性がある変更は、実行前にユーザーへ確認します。


## デザイン・ページ間の統一

HTML・CSS・JavaScriptの各学習ページは、同じWeb学習アプリとして統一感のあるデザインを維持します。

JavaScriptページを作成・編集する場合も、既存のHTML・CSSページで使用している背景・枠・カード・余白・文字色などの基本デザインを維持します。

JavaScriptの内容を追加するために、既存の枠・カード・コンテナなどを削除して、文字だけの表示へ変更しません。

既存ページで共通して使用しているCSSクラスやレイアウト構造がある場合は、可能な限りそれを再利用します。

HTML・CSS・JavaScriptのうち、特定のカテゴリだけ見た目が大きく異なる状態にしません。

新しいページを作成する場合は、既存の完成している学習ページのデザインと構造を確認してから実装します。

デザインや構造を変更する必要がある場合は、勝手に変更せず、事前にユーザーへ確認します。


## 表示・レスポンシブ対応

iPhone 16のSafariを最優先の表示確認環境とします。

既存のレスポンシブ対応を維持します。

`viewport-fit=cover` と safe-area 対応を維持します。

横スクロールや表示崩れを発生させないようにします。

iPhone 16だけでなく、iPadとPCでも大きく表示が崩れないことを確認します。

表示や操作に影響する変更を行った場合、実機確認が必要であれば、ユーザーの確認が完了するまで次の大きな変更へ進みません。


## JavaScript・localStorage

JavaScriptは、初心者が後から読み返して理解できる書き方を優先します。

必要以上にコードを短くしたり、高度な書き方へ変更したりしません。

既存のJavaScriptの動作を維持しながら変更します。

JavaScriptの機能追加を理由に、既存のHTML構造やCSSデザインを不用意に変更しません。

localStorageで使用している既存のキー名・保存形式・保存済みデータを、ユーザーの確認なしに変更・削除・初期化しません。

localStorageに関係する変更を行う場合は、既存の保存データへの影響を確認します。


## Git・GitHub

commit・push・PR作成を行いません。

Git履歴を変更する操作を行いません。

reset・rebase・履歴の書き換え・既存コミットの変更などを勝手に行いません。

変更後は、ユーザーがSourceTreeで差分を確認します。


## 作業の進め方

Version 1の完成を優先し、必要な作業を適切な単位で進めます。

大きな変更や、既存機能へ影響する変更は事前にユーザーへ確認します。

一度に必要以上の範囲を変更しません。

依頼された範囲を超えて、勝手に機能追加や大規模な整理を行いません。

分からない仕様は推測せず、ユーザーに質問します。

判断に迷う場合は、実装してから確認するのではなく、実装前に確認します。


## このプロジェクトで優先すること

1. Version 1を完成させること
2. 現在正常に動いている機能を壊さないこと
3. HTML・CSS・JavaScriptで統一されたデザインを維持すること
4. iPhone 16で正常に表示・操作できること
5. 初心者がコードを読んで理解できること
6. 保存済みのlocalStorageデータを守ること
7. 不要な変更を増やさないこと
8. Gitの履歴を安全に保つこと
